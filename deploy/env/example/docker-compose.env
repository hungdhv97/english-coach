# Docker Compose Configuration Example
# Copy this file to your environment directory (e.g., dev/docker-compose.env, prod/docker-compose.env)
# and adjust the values as needed

# Port mappings (host:container)
# These ports are used to map container ports to host ports
# Make sure ports are different for each environment if running multiple environments simultaneously
POSTGRES_PORT=5500
REDIS_PORT=6379
BACKEND_PORT=9090
FRONTEND_PORT=5173

# Docker Compose Project Name
# This will be used as prefix for container names, volumes, and networks
# Example: lexigo-dev-postgres-1, lexigo-prod-postgres-1
COMPOSE_PROJECT_NAME=lexigo-env

